{
    "title": "Cheerio Crawler input",
    "type": "object",
    "schemaVersion": 1,
    "description": "Image is...",
    "properties": {
        "datasetId": {
            "title": "Dataset or crawler execution id",
            "type": "string",
            "description": "Dataset or crawler execution...",
            "editor": "textfield"
        },
        "limit": {
            "title": "Limit items",
            "type": "integer",
            "description": "Number of items to take from the...",
            "unit": "items",
            "minimum": 0
        },
        "filterByEquality": {
            "title": "Filter by equality",
            "type": "boolean",
            "description": "If on, item will be imported if its..."
        }
    },
    "required": ["datasetId", "filterByEquality"]
}
